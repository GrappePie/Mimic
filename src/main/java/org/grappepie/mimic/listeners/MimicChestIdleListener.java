package org.grappepie.mimic.listeners;

import org.bukkit.event.Listener;

/**
 * Merged into MimicChestListener. Kept as an empty class to avoid
 * breaking any external references during the transition.
 */
public class MimicChestIdleListener implements Listener {
}
